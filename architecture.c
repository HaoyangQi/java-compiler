#include "architecture.h"

/**
 * NOTE: architecture info depends on user input, so it is
 * not part of compiler instance, and the data should be simple
 * and static, so do NOT add heap members in here
*/
